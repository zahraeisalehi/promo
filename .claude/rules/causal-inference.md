---
paths:
  - promo/baseline.py
  - promo/validate.py
  - promo/transfer.py
  - promo/audit.py
  - tests/test_synthetic.py
---

# Causal inference rules

These files implement the estimand. Correctness here is not negotiable against convenience.

## Training the counterfactual

- Fit only on rows where `treated == 0`. If a caller passes a frame containing treated rows, raise — do not filter silently, because a silent filter hides a caller bug.
- Features may not include anything derived from the promotion: no discount depth, no promo flag, no post-treatment aggregates.
- Predict `log1p(units)` and invert with `expm1`.

## Recursive rollout

For any window longer than one period, predictions must feed back as lags. Using the observed promoted value as `lag1` for week 2 causes the baseline to chase the bump and shrinks the measured effect toward zero. If you touch `rollout()`, re-run `tests/test_rollout_contamination.py`, which asserts the naive version is biased and the rollout version is not.

## Validation

Backtest error on untreated periods is necessary and not sufficient — it says nothing about `y_hat(0)` where `D = 1`. Never report predictive accuracy as evidence the effect estimate is right.

What counts:
- Synthetic truth, generated by a process the estimator does not assume. Tree models validated on tree-shaped data prove nothing (the inverse crime). The generator in `tests/synthetic.py` uses AR noise and multiplicative seasonality on purpose; do not "simplify" it.
- Placebo distribution over windows where nothing happened. This defines where zero actually sits. An estimate inside the band means no signal, which is a diagnosis about the comparison, not a verdict about the effect.

## Decomposition

`delta_q = s + (g - l)`, where `s` is expansion from the counterfactual and `g - l` is redistribution from the transfer matrix. These come from different machinery and are added. Never subtract cannibalisation from a lift that already nets it out.

The transfer matrix conserves mass: row sums and column sums total the same. Assert it. Row and column sums are identified; the cell-level split is a stated convention and must be labelled as such wherever it is displayed.
